name: Keep Render service awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -sS -o /dev/null -w "status=%{http_code}\n" --max-time 60 https://jejeboard.onrender.com/
